import { SendMail } from "../test/mail";

import { Router } from "express";
export const emailRouter = Router();

// emailRouter.post("/send-mail", SendMail);
// emailRouter.get("/", (req, res) => {
//   res.status(200).json({
//     messgae: "Hello this is the email router",
//   });
//   return;
// });
